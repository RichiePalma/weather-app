{
  "name": "weather-app",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "trufflehog": "^0.0.5"
  },
  "scripts": {
    "prepare": "husky || echo 'husky not installed, skipping'"
  },
  "lint-staged": {
    "apps/weather-backend/**/*.ts": [
      "eslint --fix --config apps/weather-backend/eslint.config.mjs",
      "prettier --write"
    ],
    "apps/weather-frontend/**/*.{ts,tsx,js,jsx}": [
      "eslint --config apps/weather-frontend/eslint.config.js --fix",
      "prettier --write"
    ]
  }
}
